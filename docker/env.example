API_SERVICE_ENV=dev
API_SERVICE_PORT=8001
API_SERVICE_LOG_LEVEL=INFO
# 宿主机数据目录真实绝对路径（DooD 沙箱挂载对齐，启用 Docker 沙箱时必填）
# 必须填写 Docker daemon 所在宿主机上的绝对路径；它应对应 Compose 的 /app/data 源目录。
# 示例：HOST_DATA_DIR=/opt/nanzi-ai-agent-platform/docker/data
# 不能只填写平台容器内路径 /app/data，也不要留空后直接使用 DooD Docker 沙箱。
HOST_DATA_DIR=
# 钉钉/企微报表简报中的完整报表链接域名
APP_PUBLIC_URL=https://yunshu.example.com

# Main database type: mysql (default)
DATABASE_TYPE=mysql

# MySQL
MYSQL_HOST=192.168.28.170
MYSQL_PORT=3306
MYSQL_DB=nanzi_ai_agent_platform
MYSQL_USER=root
MYSQL_PASSWORD=root123

# PostgreSQL (used when DATABASE_TYPE=postgresql)
POSTGRES_HOST=192.168.28.170
POSTGRES_PORT=5432
POSTGRES_DB=nanzi_ai_agent_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password

# Redis
REDIS_HOST=192.168.28.170
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=root123
REDIS_ENABLE=true

# Security-用户 API Key 的对称加密密钥（Fernet）
API_SERVICE_API_KEY_HASH_ALGORITHM=sha256
ENCRYPTION_KEY=KkJgK_d-1Jda9CAp7iGhRDzuXLYZfnid2siBeIC5lqw=


# Jira Integration

JIRA_URL=https://jira.example.com

JIRA_USERNAME=your_username

JIRA_PASSWORD=your_password
