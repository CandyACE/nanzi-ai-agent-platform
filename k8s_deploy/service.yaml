apiVersion: v1
kind: Service
metadata:
  name: nanzi-ai-agent
  namespace: nanzi-ai-agent
  labels:
    app.kubernetes.io/name: nanzi-ai-agent
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nanzi-ai-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
